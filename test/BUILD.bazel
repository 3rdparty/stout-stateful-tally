load("@rules_cc//cc:defs.bzl", "cc_test")

cc_test(
    name = "stateful-tally",
    srcs = ["stateful-tally.cc"],
    deps = [
        "//:stateful-tally",
        "@com_github_google_googletest//:gtest_main",
    ],
)
